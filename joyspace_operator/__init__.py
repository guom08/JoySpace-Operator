"""
joyspace_operator
=================
Top-level package.  Import order:
    browser  → low-level Playwright helpers (launch, auth)
    document → JoySpace document operations (read, insert, ...)
    utils    → shared helpers (logging, retry, ...)
"""
